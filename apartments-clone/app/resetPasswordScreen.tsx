import { Text } from "@ui-kitten/components";
import { Screen } from "@/components/Screen";

export default function ResetPasswordScreen({ route }: { route: { token: string } }) {
    return (
    <Screen>
        <Text>Reset Password Screen</Text>
    </Screen>
    );
};