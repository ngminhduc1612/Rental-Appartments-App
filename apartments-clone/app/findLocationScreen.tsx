import { Text } from "react-native";
import { Screen } from "@/components/Screen";

export default function FindLocationScreen() {
    return (
        <Screen>
            <Text>Find location Screen</Text>
        </Screen>
    );
};